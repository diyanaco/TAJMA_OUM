pip install virtualenv
virtualenv oum

in Bash : VSCODE will detect automatically the new env
			open a new terminal.
oum\Scripts\activate
-In PowerShell
	$env:FLASK_APP="main.py" (Only if the main file is not app.py) otherwise can run with flask run directly

pip install -r requirements.txt

flask run
